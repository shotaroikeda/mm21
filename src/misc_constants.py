"""
Holds constants used by the server and visualizer
"""

# Default server port
port = 1337

# Default map
mapFile = "/dev/null"

# Default logfile path
logFile = "/dev/null"

# Default client path
defaultClient = "/dev/null"
